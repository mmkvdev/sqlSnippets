-- command to get all the columns from a table
SELECT * FROM actor;

-- command to get only specific column from a table
SELECT first_name FROM actor;

-- command to get multiple columns from a table
SELECT first_name, last_name FROM actor;

-- command to get multiple columns from a table (order doesn't matter)
SELECT last_name, first_name FROM actor;